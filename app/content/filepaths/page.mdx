import { ArticleLayout } from "@/components/ArticleLayout";
import Markdown from "@/posts/filepaths.md";

export const article = {
  authors: "Rory Finnegan",
  date: "2019-07-23",
  title: "FilePaths: File System Abstractions and Why We Need Them",
  description:
    "Encapsulate filesystem specific logic and provide a common abstraction for interacting with various types of paths",
  format: "talk",
};

export const metadata = {
  title: article.title,
  description: article.description,
};

export default (props) => (
  <ArticleLayout article={article} {...props}>
    <Markdown />
  </ArticleLayout>
);
